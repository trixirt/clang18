- hosts: localhost
  roles:
  - role: standard-test-basic
    tags:
      - classic
    required_packages:
      - clang
      - llvm-test-suite
      - ninja-build
      - gcc-x86_64-linux-gnu
    tests:
      - rhbz#482491:
          dir: ./
          run: echo "int main(){}" | clang -x c -
      - llvm-test-suite
      - llvm-abi-test-suite
      - rhbz_1657544
